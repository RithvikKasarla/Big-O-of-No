"use client";
import React from "react";

const Math = () => {
  const name = "csds";
  const key = "csds";

  return <>{/* <NotePageTemplate name={name} key={key} /> */}</>;
};

export default Math;
